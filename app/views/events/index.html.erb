<%= link_to 'Create New Event', new_event_path %>

<% @events.each do |event| %>
  <li>
    <%= event.id %>
    <%= link_to 'Edit', edit_event_path(event) %>
    <%= link_to 'Delete', event, method: :delete, data: { confirm: 'Are you sure you want to delete this alert?' } %>
  </li>
<% end %>
